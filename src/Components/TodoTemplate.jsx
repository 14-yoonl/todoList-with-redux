import React from "react";
import { Template, Title } from "../Styled/common-styled";

const TodoTemplate = () => {
  return (
    <Template>
      <Title> 할 일 </Title>
    </Template>
  );
};

export default TodoTemplate;
